import 'package:flutter/material.dart';

class AppColors {
  // Cores principais da nova paleta
  static const Color background = Color(0xFFFFFFFF); // branco
  static const Color white = Color(0xFFFFFFFF); // branco
  static const Color background2 = Color(0xFFBFD989); // Verde claro
  static const Color primary = Color(0xFF95BF3B); // Verde vibrante
  static const Color secondary = Color(0xFF021F59); // Azul escuro
  static const Color accent = Color(0xFF023059); // Azul médio
  static const Color light = Color(0xFFEEF8DA); // Verde claro (pode ser usado como light)
  static const Color darkText = Color(0xFF0D0D0D); // Quase preto para textos

  // Cores de botões
  static const Color buttonBackground = primary;
  static const Color buttonText = Colors.white;

  // Cores de campos de input
  static const Color inputBorder = Color(0xFF023059); // Azul médio
  static const Color inputFill = light;

  // Texto
  static const Color textPrimary = darkText;
  static const Color textSecondary = Color(0xFF021F59); // Azul escuro

  // Ícones e Destaques
  static const Color icon = secondary;
  static const Color highlight = accent;

  // Erros ou alertas
  static const Color warning = Color(0xFFE57300); // tom alaranjado mais forte
}
